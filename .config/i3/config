# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

#Border color stuff
client.focused       #FFFFFF #FFFFFF #000000 #DDDDDD #FFFFFF

#Take off window titles
for_window [class="^.*"] border pixel 30

#Attempt to emulate binary insert
for_window [class="^.*"] split toggle

#Background generation
#exec --no-startup-id exec 

# Gaps stuff
gaps inner 100
gaps outer 0

#set $alt Mod1
#set $cmd Mod4
#Fix this by remapping keys on keyboard
set $alt Mod4
set $cmd Mod1
set $ctl Control

font pango:DejaVu Sans Mono 18

# Use Mouse+$cmd to drag floating windows to their wanted position
floating_modifier $alt

# start a terminal
bindsym $cmd+Return exec termite
bindsym $cmd+Shift+Return exec emacsclient -c -a '' -e '(eshell)'

#Applications
#Files
bindsym $cmd+f exec termite -e vifm
#Database applications
#$bindsym $cmd+d 
#Emacs
bindsym $cmd+e exec emacsclient -c -a '' -e '(spacemacs/home)'
#Intellij
bindsym $cmd+i exec idea
#bindsym $cmd+i
#Chrome
bindsym $cmd+g exec chromium
bindsym $cmd+Shift+g exec chromium --incognito
#Vim
bindsym $cmd+v exec termite -e vim
#htop
bindsym $cmd+h exec termite -e htop
#hipchat
#bindsym $cmd+h
#Email (outlook)
#bindsym $cmd+o
#Jira
#bindsym $cmd+j
#Messaging (pidgin)
#bindsym $cmd+p
#Pomodoro timer
#bindsym $cmd+m
#Time logger
#bindsym $cmd+t
#Calculator
bindsym $cmd+c emacsclient -a '' -n -c -e '(full-calc)'
# open ssh connection
bindsym $cmd+s exec rofi -show ssh

# kill focused window
bindsym $cmd+w kill

# start rofi (a program launcher)
bindsym $cmd+space exec rofi -show run

# change focus
bindsym $alt+h focus left
bindsym $alt+j focus down
bindsym $alt+k focus up
bindsym $alt+l focus right

# move focused window
bindsym $alt+Shift+h move left
bindsym $alt+Shift+j move down
bindsym $alt+Shift+k move up
bindsym $alt+Shift+l move right

# enter fullscreen mode for the focused container
bindsym $alt+f fullscreen toggle
bindsym $alt+d layout toggle
bindsym $alt+a exec /home/michael/.bin/toggle-gaps

# change container layout (stacked, tabbed, toggle split)
bindsym $alt+s layout stacking
bindsym $alt+w layout tabbed
#bindsym $alt+Shift+E layout toggle split

# toggle tiling / floating
bindsym $alt+space floating toggle
# change focus between tiling / floating windows
#bindsym $cmd+space focus mode_toggle

# focus the parent container
bindsym $alt+p focus parent
# focus the child container
#bindsym $cmd+d focus child


# switch to workspace
bindsym $alt+1 workspace 1
bindsym $alt+2 workspace 2
bindsym $alt+3 workspace 3
bindsym $alt+4 workspace 4
bindsym $alt+5 workspace 5
bindsym $alt+6 workspace 6
bindsym $alt+7 workspace 7
bindsym $alt+8 workspace 8
bindsym $alt+9 workspace 9
bindsym $alt+0 workspace 10

# move focused container to workspace
bindsym $alt+Shift+1 move container to workspace 1
bindsym $alt+Shift+2 move container to workspace 2
bindsym $alt+Shift+3 move container to workspace 3
bindsym $alt+Shift+4 move container to workspace 4
bindsym $alt+Shift+5 move container to workspace 5
bindsym $alt+Shift+6 move container to workspace 6
bindsym $alt+Shift+7 move container to workspace 7
bindsym $alt+Shift+8 move container to workspace 8
bindsym $alt+Shift+9 move container to workspace 9
bindsym $alt+Shift+0 move container to workspace 10

# move focused container to workspace, and switch to workspace
bindsym $cmd+Shift+1 move container to workspace 1, workspace 1
bindsym $cmd+Shift+2 move container to workspace 2, workspace 2
bindsym $cmd+Shift+3 move container to workspace 3, workspace 3
bindsym $cmd+Shift+4 move container to workspace 4, workspace 4
bindsym $cmd+Shift+5 move container to workspace 5, workspace 5
bindsym $cmd+Shift+6 move container to workspace 6, workspace 6
bindsym $cmd+Shift+7 move container to workspace 7, workspace 7
bindsym $cmd+Shift+8 move container to workspace 8, workspace 8
bindsym $cmd+Shift+9 move container to workspace 9, workspace 9
bindsym $cmd+Shift+0 move container to workspace 10, workspace 10

#cycle workspaces
bindsym $ctl+$alt+Left workspace prev
bindsym $ctl+$alt+Right workspace next

# reload the configuration file
bindsym $cmd+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $cmd+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $cmd+Shift+O exec exit
bindsym $cmd+Shift+o exec "i3-nagbar -t warning -m 'Are you sure you want to log out?' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $cmd+Shift+p exec systemctl poweroff -i
bindsym $cmd+l exec i3lock

#Resize focused window
bindsym $alt+Shift+a resize shrink width 10px or 10ppt
bindsym $alt+Shift+s resize grow height 10px or 10ppt
bindsym $alt+Shift+d resize shrink height 10px or 10ppt
bindsym $alt+Shift+f resize grow width 10px or 10ppt

#TODO Reconsider these
bindsym $cmd+Shift+a resize grow width 10px or 10ppt
bindsym $cmd+Shift+s resize shrink height 10px or 10ppt
bindsym $cmd+Shift+d resize grow height 10px or 10ppt
bindsym $cmd+Shift+f resize shrink width 10px or 10ppt

# split in horizontal orientation
bindsym $alt+u split h
# split in vertical orientation
bindsym $alt+v split v
# toggle split
bindsym $alt+e layout toggle split

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#        tray_output primary
#}

#exec compton --config /home/michael/.config/compton.conf -b
exec compton --backend glx --paint-on-overlay --vsync opengl-swc
exec /home/michael/.fehbg
exec polybar example
exec termite
exec redshift
